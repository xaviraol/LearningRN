import React, { Component } from 'react';
import { Text, ScrollView, StyleSheet } from 'react-native';

export default class extends Component<undefined, undefined> {
    render() {
        return <ScrollView>
            <Text> The first screen </Text>
            </ScrollView>
        ;
    }
}
